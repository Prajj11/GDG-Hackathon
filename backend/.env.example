DATABASE_URL=sqlite:///./digital_guardrails.db
DETECTOR=auto
MODEL_DIR=backend/artifacts/indicbert
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Bind to loopback for the unauthenticated synthetic-data demo.
# Required shared access token for an externally hosted demo; never commit a value.
DEMO_ACCESS_TOKEN=
